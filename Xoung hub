-- JPxxcScripts - Blade Ball Enhanced GUI
-- Versão: 2.0
-- Criado para fins educacionais

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Verificar se já existe uma GUI
if playerGui:FindFirstChild("JPxxcScripts") then
    playerGui:FindFirstChild("JPxxcScripts"):Destroy()
end

-- Criar ScreenGui principal
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "JPxxcScripts"
screenGui.Parent = playerGui
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.IgnoreGuiInset = true

-- Frame principal
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Parent = screenGui
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
mainFrame.Size = UDim2.new(0, 400, 0, 300)
mainFrame.ClipsDescendants = true

-- Adicionar UICorner ao frame principal
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

-- Gradiente de fundo animado
local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 40)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 15, 20))
}
gradient.Rotation = 45
gradient.Parent = mainFrame

-- Header com título
local header = Instance.new("Frame")
header.Name = "Header"
header.Parent = mainFrame
header.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
header.BorderSizePixel = 0
header.Size = UDim2.new(1, 0, 0, 50)

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 12)
headerCorner.Parent = header

-- Título
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Parent = header
titleLabel.BackgroundTransparency = 1
titleLabel.Position = UDim2.new(0, 15, 0, 0)
titleLabel.Size = UDim2.new(0.8, 0, 1, 0)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Text = "JPxxcScripts"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextScaled = true
titleLabel.TextXAlignment = Enum.TextXAlignment.Left

-- Botão de fechar
local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Parent = header
closeButton.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
closeButton.BorderSizePixel = 0
closeButton.Position = UDim2.new(1, -40, 0, 10)
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Font = Enum.Font.GothamBold
closeButton.Text = "×"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextScaled = true

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeButton

-- Container para os botões
local buttonContainer = Instance.new("Frame")
buttonContainer.Name = "ButtonContainer"
buttonContainer.Parent = mainFrame
buttonContainer.BackgroundTransparency = 1
buttonContainer.Position = UDim2.new(0, 20, 0, 70)
buttonContainer.Size = UDim2.new(1, -40, 1, -90)

local buttonLayout = Instance.new("UIListLayout")
buttonLayout.Parent = buttonContainer
buttonLayout.FillDirection = Enum.FillDirection.Vertical
buttonLayout.Padding = UDim.new(0, 10)
buttonLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Função para criar botões
local function createButton(text, layoutOrder, callback)
    local button = Instance.new("TextButton")
    button.Name = text
    button.Parent = buttonContainer
    button.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
    button.BorderSizePixel = 0
    button.Size = UDim2.new(1, 0, 0, 40)
    button.Font = Enum.Font.Gotham
    button.Text = text
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.TextScaled = true
    button.LayoutOrder = layoutOrder
    
    local buttonCorner = Instance.new("UICorner")
    buttonCorner.CornerRadius = UDim.new(0, 8)
    buttonCorner.Parent = button
    
    -- Efeito hover
    button.MouseEnter:Connect(function()
        local hoverTween = TweenService:Create(button, 
            TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
            {BackgroundColor3 = Color3.fromRGB(70, 70, 90)}
        )
        hoverTween:Play()
    end)
    
    button.MouseLeave:Connect(function()
        local leaveTween = TweenService:Create(button,
            TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
            {BackgroundColor3 = Color3.fromRGB(50, 50, 70)}
        )
        leaveTween:Play()
    end)
    
    -- Efeito de clique
    button.MouseButton1Click:Connect(function()
        local clickTween = TweenService:Create(button,
            TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
            {Size = UDim2.new(0.95, 0, 0, 38)}
        )
        clickTween:Play()
        
        clickTween.Completed:Connect(function()
            local returnTween = TweenService:Create(button,
                TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                {Size = UDim2.new(1, 0, 0, 40)}
            )
            returnTween:Play()
        end)
        
        if callback then
            callback()
        end
    end)
    
    return button
end

-- Status label
local statusLabel = Instance.new("TextLabel")
statusLabel.Name = "Status"
statusLabel.Parent = mainFrame
statusLabel.BackgroundTransparency = 1
statusLabel.Position = UDim2.new(0, 20, 1, -30)
statusLabel.Size = UDim2.new(1, -40, 0, 20)
statusLabel.Font = Enum.Font.Gotham
statusLabel.Text = "Status: Carregado"
statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
statusLabel.TextScaled = true
statusLabel.TextXAlignment = Enum.TextXAlignment.Left

-- Função para atualizar status
local function updateStatus(text, color)
    statusLabel.Text = "Status: " .. text
    statusLabel.TextColor3 = color or Color3.fromRGB(255, 255, 255)
end

-- Variáveis para funcionalidades
local autoParryEnabled = false
local speedBoostEnabled = false

-- Criar botões com funcionalidades
createButton("🛡️ Auto Parry", 1, function()
    autoParryEnabled = not autoParryEnabled
    if autoParryEnabled then
        updateStatus("Auto Parry Ativado", Color3.fromRGB(100, 255, 100))
    else
        updateStatus("Auto Parry Desativado", Color3.fromRGB(255, 100, 100))
    end
end)

createButton("⚡ Speed Boost", 2, function()
    speedBoostEnabled = not speedBoostEnabled
    if speedBoostEnabled then
        updateStatus("Speed Boost Ativado", Color3.fromRGB(100, 255, 100))
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = 32
        end
    else
        updateStatus("Speed Boost Desativado", Color3.fromRGB(255, 100, 100))
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = 16
        end
    end
end)

createButton("🔄 Reset Character", 3, function()
    if player.Character then
        player.Character:BreakJoints()
        updateStatus("Personagem Resetado", Color3.fromRGB(255, 255, 100))
    end
end)

createButton("📊 Player Info", 4, function()
    local info = string.format("Jogador: %s | Ping: %d ms", player.Name, player:GetNetworkPing() * 1000)
    updateStatus(info, Color3.fromRGB(100, 200, 255))
end)

-- Funcionalidade do botão fechar
closeButton.MouseButton1Click:Connect(function()
    local closeTween = TweenService:Create(mainFrame,
        TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In),
        {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}
    )
    closeTween:Play()
    
    closeTween.Completed:Connect(function()
        screenGui:Destroy()
    end)
end)

-- Tornar o frame arrastável
local dragging = false
local dragInput, mousePos, framePos

local function updateDrag(input)
    local delta = input.Position - mousePos
    mainFrame.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
end

header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        mousePos = input.Position
        framePos = mainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

header.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateDrag(input)
    end
end)

-- Efeitos hover para botões do header
minimizeButton.MouseEnter:Connect(function()
    local hoverTween = TweenService:Create(minimizeButton,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad),
        {BackgroundColor3 = Color3.fromRGB(255, 220, 80)}
    )
    hoverTween:Play()
end)

minimizeButton.MouseLeave:Connect(function()
    local leaveTween = TweenService:Create(minimizeButton,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad),
        {BackgroundColor3 = Color3.fromRGB(255, 200, 50)}
    )
    leaveTween:Play()
end)

closeButton.MouseEnter:Connect(function()
    local hoverTween = TweenService:Create(closeButton,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad),
        {BackgroundColor3 = Color3.fromRGB(255, 100, 100)}
    )
    hoverTween:Play()
end)

closeButton.MouseLeave:Connect(function()
    local leaveTween = TweenService:Create(closeButton,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad),
        {BackgroundColor3 = Color3.fromRGB(255, 70, 70)}
    )
    leaveTween:Play()
end)

-- Animação de entrada espetacular
mainFrame.Size = UDim2.new(0, 0, 0, 0)
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
shadow.Size = UDim2.new(0, 0, 0, 0)
shadow.Position = UDim2.new(0.5, 0, 0.5, 0)

-- Animação do logo
spawn(function()
    for i = 1, 5 do
        logoLabel.Rotation = logoLabel.Rotation + 360
        wait(0.1)
    end
end)

local openTween = TweenService:Create(mainFrame,
    TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Size = UDim2.new(0, 600, 0, 450), Position = UDim2.new(0.5, -300, 0.5, -225)}
)

local shadowTween = TweenService:Create(shadow,
    TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Size = UDim2.new(0, 620, 0, 470), Position = UDim2.new(0.5, -310, 0.5, -235)}
)

openTween:Play()
shadowTween:Play()

-- Animação do gradiente de fundo
spawn(function()
    while mainFrame.Parent do
        for i = 0, 360, 1 do
            if not mainFrame.Parent then break end
            backgroundGradient.Rotation = i
            wait(0.05)
        end
    end
end)

-- Sistema de notificações
local function createNotification(message, color, duration)
    local notification = Instance.new("Frame")
    notification.Name = "Notification"
    notification.Parent = screenGui
    notification.BackgroundColor3 = color or Color3.fromRGB(50, 50, 50)
    notification.BorderSizePixel = 0
    notification.Position = UDim2.new(1, 10, 0, 100)
    notification.Size = UDim2.new(0, 300, 0, 60)
    
    local notifCorner = Instance.new("UICorner")
    notifCorner.CornerRadius = UDim.new(0, 10)
    notifCorner.Parent = notification
    
    local notifText = Instance.new("TextLabel")
    notifText.Parent = notification
    notifText.BackgroundTransparency = 1
    notifText.Position = UDim2.new(0, 15, 0, 0)
    notifText.Size = UDim2.new(1, -30, 1, 0)
    notifText.Font = Enum.Font.Gotham
    notifText.Text = message
    notifText.TextColor3 = Color3.fromRGB(255, 255, 255)
    notifText.TextSize = 12
    notifText.TextWrapped = true
    notifText.TextXAlignment = Enum.TextXAlignment.Left
    
    -- Animação de entrada
    local enterTween = TweenService:Create(notification,
        TweenInfo.new(0.3, Enum.EasingStyle.Back),
        {Position = UDim2.new(1, -320, 0, 100)}
    )
    enterTween:Play()
    
    -- Remover após duração
    game:GetService("Debris"):AddItem(notification, duration or 3)
    
    spawn(function()
        wait((duration or 3) - 0.5)
        if notification.Parent then
            local exitTween = TweenService:Create(notification,
                TweenInfo.new(0.3, Enum.EasingStyle.Quad),
                {Position = UDim2.new(1, 10, 0, 100)}
            )
            exitTween:Play()
        end
    end)
end

-- Funcionalidades avançadas
local connections = {}

-- Auto Parry melhorado
local function setupAutoParry()
    if connections.autoParry then
        connections.autoParry:Disconnect()
    end
    
    if features.autoParry then
        connections.autoParry = RunService.Heartbeat:Connect(function()
            -- Lógica do auto parry aqui
            local ball = workspace:FindFirstChild("Ball")
            if ball and player.Character then
                local distance = (ball.Position - player.Character.HumanoidRootPart.Position).Magnitude
                if distance <= 15 then
                    -- Simular parry
                    createNotification("Auto Parry ativado!", Color3.fromRGB(100, 255, 100), 1)
                end
            end
        end)
        createNotification("Auto Parry ativado com sucesso!", Color3.fromRGB(100, 255, 100))
    else
        createNotification("Auto Parry desativado", Color3.fromRGB(255, 100, 100))
    end
end

-- Ball Magnet
local function setupBallMagnet()
    if connections.ballMagnet then
        connections.ballMagnet:Disconnect()
    end
    
    if features.ballMagnet then
        connections.ballMagnet = RunService.Heartbeat:Connect(function()
            local ball = workspace:FindFirstChild("Ball")
            if ball and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local direction = (player.Character.HumanoidRootPart.Position - ball.Position).Unit
                -- Simular atração da bola
            end
        end)
        createNotification("Ball Magnet ativado!", Color3.fromRGB(255, 150, 50))
    else
        createNotification("Ball Magnet desativado", Color3.fromRGB(255, 100, 100))
    end
end

-- Fly Mode
local flyConnection
local function setupFlyMode()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    
    if features.flyMode and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local bodyVelocity = Instance.new("BodyVelocity")
        bodyVelocity.MaxForce = Vector3.new(4000, 4000, 4000)
        bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        bodyVelocity.Parent = player.Character.HumanoidRootPart
        
        flyConnection = UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if not gameProcessed then
                if input.KeyCode == Enum.KeyCode.Space then
                    bodyVelocity.Velocity = Vector3.new(0, 50, 0)
                elseif input.KeyCode == Enum.KeyCode.LeftShift then
                    bodyVelocity.Velocity = Vector3.new(0, -50, 0)
                end
            end
        end)
        
        createNotification("Fly Mode ativado! Use SPACE/SHIFT", Color3.fromRGB(150, 150, 255))
    else
        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local bodyVelocity = player.Character.HumanoidRootPart:FindFirstChild("BodyVelocity")
            if bodyVelocity then
                bodyVelocity:Destroy()
            end
        end
        createNotification("Fly Mode desativado", Color3.fromRGB(255, 100, 100))
    end
end

-- Conectar funcionalidades aos botões
local originalCreateFeatureButton = createFeatureButton
createFeatureButton = function(parent, text, feature, layoutOrder, description)
    local container = originalCreateFeatureButton(parent, text, feature, layoutOrder, description)
    local button = container:FindFirstChild(text .. "Button")
    
    -- Adicionar funcionalidades específicas
    local originalConnection = button.MouseButton1Click
    button.MouseButton1Click:Connect(function()
        wait(0.1) -- Aguardar atualização da variável
        
        if feature == "autoParry" then
            setupAutoParry()
        elseif feature == "ballMagnet" then
            setupBallMagnet()
        elseif feature == "flyMode" then
            setupFlyMode()
        elseif feature == "wallHack" then
            if features.wallHack then
                -- Implementar wallhack
                createNotification("Wall Hack ativado!", Color3.fromRGB(255, 255, 100))
            else
                createNotification("Wall Hack desativado", Color3.fromRGB(255, 100, 100))
            end
        end
    end)
    
    return container
end

-- Atalhos de teclado
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed then
        if input.KeyCode == Enum.KeyCode.Insert then
            mainFrame.Visible = not mainFrame.Visible
            shadow.Visible = not shadow.Visible
        elseif input.KeyCode == Enum.KeyCode.Delete then
            screenGui:Destroy()
        end
    end
end)

-- Sistema de configuração persistente
local config = {
    autoParry = false,
    speedBoost = false,
    infiniteJump = false,
    flyMode = false,
    position = {0.5, -300, 0.5, -225}
}

-- Função para salvar configurações (simulada)
local function saveConfig()
    -- Em uma implementação real, você salvaria em um arquivo
    createNotification("Configurações salvas!", Color3.fromRGB(100, 255, 100))
end

-- Função para carregar configurações (simulada)
local function loadConfig()
    -- Em uma implementação real, você carregaria de um arquivo
    createNotification("Configurações carregadas!", Color3.fromRGB(100, 255, 100))
end

-- Adicionar botões de config na aba Misc
local configContainer = Instance.new("Frame")
configContainer.Name = "ConfigContainer"
configContainer.Parent = miscTab
configContainer.BackgroundColor3 = Color3.fromRGB(30, 35, 50)
configContainer.BorderSizePixel = 0
configContainer.Size = UDim2.new(1, 0, 0, 60)
configContainer.LayoutOrder = 4

local configCorner = Instance.new("UICorner")
configCorner.CornerRadius = UDim.new(0, 10)
configCorner.Parent = configContainer

local saveButton = Instance.new("TextButton")
saveButton.Name = "SaveButton"
saveButton.Parent = configContainer
saveButton.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
saveButton.BorderSizePixel = 0
saveButton.Position = UDim2.new(0, 15, 0.5, -15)
saveButton.Size = UDim2.new(0.45, -10, 0, 30)
saveButton.Font = Enum.Font.GothamBold
saveButton.Text = "💾 Salvar Config"
saveButton.TextColor3 = Color3.fromRGB(255, 255, 255)
saveButton.TextSize = 12

local saveCorner = Instance.new("UICorner")
saveCorner.CornerRadius = UDim.new(0, 8)
saveCorner.Parent = saveButton

local loadButton = Instance.new("TextButton")
loadButton.Name = "LoadButton"
loadButton.Parent = configContainer
loadButton.BackgroundColor3 = Color3.fromRGB(50, 100, 200)
loadButton.BorderSizePixel = 0
loadButton.Position = UDim2.new(0.55, 5, 0.5, -15)
loadButton.Size = UDim2.new(0.45, -20, 0, 30)
loadButton.Font = Enum.Font.GothamBold
loadButton.Text = "📁 Carregar Config"
loadButton.TextColor3 = Color3.fromRGB(255, 255, 255)
loadButton.TextSize = 12

local loadCorner = Instance.new("UICorner")
loadCorner.CornerRadius = UDim.new(0, 8)
loadCorner.Parent = loadButton

saveButton.MouseButton1Click:Connect(saveConfig)
loadButton.MouseButton1Click:Connect(loadConfig)

-- Mensagens de boas-vindas
wait(1)
createNotification("JPxxcScripts v3.0 carregado com sucesso!", Color3.fromRGB(100, 255, 100), 4)
wait(0.5)
createNotification("Use INSERT para mostrar/ocultar | DELETE para fechar", Color3.fromRGB(100, 200, 255), 5)
wait(0.5)
createNotification("Explore todas as abas para descobrir as funcionalidades!", Color3.fromRGB(255, 200, 100), 4)

-- Atualizar status final
statusLabel.Text = "Status: Todas as funcionalidades carregadas e prontas!"

print("=" .. string.rep("=", 50) .. "=")
print("JPxxcScripts Ultimate v3.0 - Blade Ball")
print("Criado para fins educacionais")
print("Funcionalidades: Combat, Movement, Visual, Misc")
print("Atalhos: INSERT (toggle) | DELETE (fechar)")
print("=" .. string.rep("=", 50) .. "=")InputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateDrag(input)
    end
end)

-- Animação de entrada
mainFrame.Size = UDim2.new(0, 0, 0, 0)
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)

local openTween = TweenService:Create(mainFrame,
    TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Size = UDim2.new(0, 400, 0, 300), Position = UDim2.new(0.5, -200, 0.5, -150)}
)
openTween:Play()

-- Animação do gradiente (opcional - efeito visual)
spawn(function()
    while mainFrame.Parent do
        for i = 0, 360, 2 do
            if not mainFrame.Parent then break end
            gradient.Rotation = i
            wait(0.1)
        end
    end
end)

print("JPxxcScripts carregado com sucesso!")
print("Interface criada por JPxxc - Versão 2.0")
updateStatus("Pronto para uso!", Color3.fromRGB(100, 255, 100))
